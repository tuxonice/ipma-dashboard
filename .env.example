APP_ENV=dev
APP_DEBUG=1

# Public origin of the site (scheme + host, no trailing slash).
# Used by bin/update-seo-urls.php to stamp public/sitemap.xml and robots.txt.
APP_URL=https://ipma-dashboard.tlab.pt